# Cloudflare workflow disabled – project is deployed on Railway
# Railway auto-deploys from GitHub on every push to master.
# This file is kept to avoid breaking existing GitHub Actions references.

name: CI – Lint check (placeholder)

on:
  push:
    branches: ["master"]

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment info
        run: |
          echo "✅ Code pushed to master."
          echo "🚂 Railway will auto-deploy this commit."
          echo "📌 Cloudflare deployment is disabled (Flask backend not supported)."
